# CodeLens AI

AI Code Review 平台的前端工程基线。

## 本地开发

```bash
npm install
npm run dev
```

## 质量检查

```bash
npm run lint
npm run typecheck
npm run format:check
npm run build
```

环境变量以 `.env.example` 为模板，并使用 `VITE_` 前缀暴露给客户端。
